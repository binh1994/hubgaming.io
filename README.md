# HubGaming (Jekyll on Netlify/GitHub)

- Neon gaming theme (purple/cyan), SEO ready, OG meta, sitemap.
- Daily auto-post via GitHub Actions at 09:30 UTC.
- Ads + Google Analytics already integrated.

## Local run
```bash
bundle install
bundle exec jekyll serve
